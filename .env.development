# Committed to git — safe to share (no secrets)
# Controls whether the waitlist landing page is shown instead of the app.
# Set VITE_SET_WAITLIST=true in Vercel to lock production to the landing page.
VITE_SET_WAITLIST=false
